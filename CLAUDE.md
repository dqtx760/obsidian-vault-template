# 语言设定与核心角色 (Global Rules)

- **语言指令**：无论输入何种语言，你必须始终使用**简体中文**进行思考、回复和知识库的编写。
- **角色定义**：你正在维护一个 **LLM Wiki**（基于 Andrej Karpathy 的知识管理方法论），你的任务是将碎片化的信息编译成结构化、高度相互链接的 Obsidian 知识库。

---

# 核心目录与权限边界 (Immutability & Architecture)

你必须严格遵守以下文件操作权限，这是不可逾越的底线：

- `01-输入/` (不可变层 - Immutable)：
  - **绝对只读**。这里存放原始素材、网页剪藏、论文、转录稿。
  - **禁止修改或删除此目录下的任何文件**。它是事实的唯一真相来源。
  - 处理完成后，将源文件移动至 `01-输入/09-archive/` 归档。
- `02-处理中/` (中间处理层)：
  - 存放研究摘要、大纲、草稿等中间产物。
  - 临时工作区，处理完成后内容应编译到 wiki 中。
- `03-输出/wiki/` (编译输出层 - You Own This)：
  - 这是你的专属工作区。你需要在此处创建、更新、提炼知识并解决矛盾。
  - 所有生成的知识页面必须放在此目录下对应的子文件夹中。
- `04-反馈/` (反馈层)：
  - 存放数据指标、用户反馈、复盘记录，驱动系统进化。
- `05-参考/` (长期参考)：
  - 存放提示词库、使用指南、参考文档等长期参考资料。
- `.claude/` (Claude Code 配置)：
  - 存放技能配置、记忆文件、自动化钩子等。

---

# Wiki 核心文件契约 (The Wiki Schema)

当你在 `03-输出/wiki/` 中工作时（尤其是执行写入操作后），必须维护以下基石：

1. **`03-输出/wiki/index.md` (总目录)**：
   每次向 wiki 新增知识页后，必须同步更新此文件，将其按分类加入目录中。
   格式要求： `[[页面名称]] — 一句话描述`。
   - Entities/Concepts: 使用 TitleCase 命名，下划线分隔单词。
   - Sources/Syntheses: 使用短横线命名。
   范例：
   ```markdown
   # Wiki Index

   ## Sources
   - [[摘要-karpathy-llm-wiki]] — Andrej Karpathy LLM Wiki 方法论。

   ## Entities
   - [[Andrej_Karpathy]] — 前 Tesla AI 总监，LLM Wiki 提出者。

   ## Concepts
   - [[LLM_Wiki_Methodology]] — Karpathy 提出的知识管理完整框架。

   ## Syntheses
   - [[综合-xxx-分析]] — 该页面回答的复杂问题。
   ```

2. **`03-输出/wiki/log.md` (操作日志)**：
   只能追加写入（Append-only）。每次操作后记录：`## [YYYY-MM-DD] <动作> | <操作简述>`。
   操作类型： ingest, query, lint, sync, cleanup
   范例：
   ```markdown
   ## [2026-05-20] ingest | 摄入 Karpathy LLM Wiki 方法论文章
   - **变更**: 新增 [[Andrej_Karpathy]], [[摘要-karpathy-llm-wiki]]; 更新 [[index.md]]
   - **冲突**: 无 (或: 冲突 [[知识冲突页面]], 已标注)

   ## [2026-05-20] query | 解析某个知识体系
   - **输出**: 已保存至 [[综合-xxx-分析]]

   ## [2026-05-20] lint | 健康检查
   - **结果**: 修复 2 处死链，发现 1 个孤儿页面
   ```

3. **内容分类**：
   - `03-输出/wiki/concepts/`：存放概念、框架、方法论（如 `LLM_Wiki_Methodology.md`）。
   - `03-输出/wiki/entities/`：存放人物、公司、工具、产品（如 `Andrej_Karpathy.md`）。
   - `03-输出/wiki/sources/`：存放从 `01-输入/` 提炼出的原始素材摘要。
   - `03-输出/wiki/syntheses/`：存放跨来源的综合分析、深度总结。

4. **强制双向链接**：
   每一个 wiki 页面必须包含 `## 关联连接` 区域，使用 Obsidian 双链 `[[页面名称]]` 链接到其他相关概念。绝不能产生孤岛页面。

5. **矛盾处理原则**：
   如果新摄入的知识与旧知识冲突，不要静默覆盖。在页面中新建 `## 知识冲突` 区块，将两种说法都保留并做对比。

---

# 工作流指令说明 (Workflows / Skills)

本知识库已配置三个核心技能，当被要求执行以下操作时，请遵循：

- `/ingest <路径>`：读取指定的 `01-输入/` 文件，将其核心价值提炼并整合到 `03-输出/wiki/` 目录的相关概念/实体中。必须更新 index 和 log。
- `/query <问题>`：通过读取 `03-输出/wiki/index.md` 寻找相关文件，进行深度阅读后综合回答，并在回答中必须使用 `[[wikilink]]` 标注引用来源。
- `/lint`：全局扫描 `03-输出/wiki/` 目录，找出孤岛页面（没有双链）、死链（链接不存在的页面）以及存在逻辑冲突的地方，并向我报告。

---

# 页面 Frontmatter (YAML) 规范

所有生成的 wiki 页面必须包含以下 YAML 头部：
```yaml
---
title: "页面标题"
type: concept | entity | source | synthesis
tags: [知识标签]
sources: [关联的源文件相对路径]
last_updated: YYYY-MM-DD
confidence: high | medium | low
---
```

---

# 知识编译原则

1. **原始资料是唯一事实来源**：所有生成的 wiki 内容必须能在 `01-输入/` 的原始资料中找到对应依据。
2. **增量更新而非覆盖**：已有页面只追加真正的新信息，不覆盖原有内容。宁可重复，不要丢失。
3. **无对应来源不保留**：如果某个知识页面找不到对应的原始资料来源，应在 lint 时标记并清理。
4. **所有操作必须记日志**：审计可追溯，Append-only 不改历史。
